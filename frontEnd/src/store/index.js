import { createStore } from 'vuex'
import movieStore from './modules/movieStore'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    movieStore
  }
})
